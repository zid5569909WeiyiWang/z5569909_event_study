# toolkit_config.py

import os

# This sets the data_folder variable to point to the 'data' subfolder
data_folder = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'data')

# Add BASEDIR variable to point to the project's root directory
BASEDIR = os.path.dirname(os.path.abspath(__file__))
